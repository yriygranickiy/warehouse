version: '3.0'
services:
  warehouse_db:
    container_name: warehouse_db
    image: postgres:14.4-alpine
    environment:
      POSTGRES_USER: my_user
      POSTGRES_PASSWORD: qwerty
      POSTGRES_DB: my_db
    ports:
      - "5438:5432"